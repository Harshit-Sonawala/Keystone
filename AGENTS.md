# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v56.0.0/ before writing any code.

# Typography Rule
Never use `font-extrabold` or `font-bold` in styling/typography unless explicitly specified by the user.

